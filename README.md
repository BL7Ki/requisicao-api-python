# requisicao-api-python

pip install requests
import requests

Lembrando que o código da api-python deve estar rodando no seu pc, depois disso conseguimos acessar a informação com o nome da rota /totalvendas